name: Live Scoreboard Refresh

on:
  # Runs at the top of every hour (UTC)
  schedule:
    - cron: "0 * * * *"
  # Lets you run it by hand anytime
  workflow_dispatch:

# Allow this workflow to commit/push
permissions:
  contents: write

# Prevent overlapping runs if one is still going when the next hour hits
concurrency:
  group: live-scoreboard-refresh
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pandas requests

      # Heartbeat so you can see it fired and when (UTC)
      - name: Cron heartbeat
        run: |
          echo "⏰ Cron fired at: $(date -u +'%Y-%m-%d %H:%M:%S') UTC"
          echo "Tip: UTC = ET + 4 (during Daylight Time)."

      # 1) Check NFL game status and capture active week
      - name: Check if games are active
        id: gamestatus
        run: python NFLgameStatus.py

      - name: Read NFL week number
        if: ${{ success() }}
        run: echo "WEEK_NUM=$(jq -r '.week_number' nfl_status.json)" >> $GITHUB_ENV

      # 2) Refresh scores for active week only
      - name: Refresh Scores for active week
        if: ${{ success() }}
        run: python score_details.py --week $WEEK_NUM

      # 3) Commit updated CSV and LastUpdate
      - name: Commit updated data
        if: ${{ success() }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/Scores.csv data/LastUpdate.csv || true
          git commit -m "Auto update live scores (Week $WEEK_NUM)" || echo "No changes to commit"
          git push || echo "Nothing to push"

      # 4) Trigger Power BI dataset refresh
      - name: Trigger Power BI dataset refresh
        if: ${{ success() }}
        env:
          TENANT_ID: ${{ secrets.PBI_TENANT_ID }}
          CLIENT_ID: ${{ secrets.PBI_CLIENT_ID }}
          CLIENT_SECRET: ${{ secrets.PBI_CLIENT_SECRET }}
          WORKSPACE_ID: ${{ secrets.PBI_WORKSPACE_ID }}
          DATASET_ID: ${{ secrets.PBI_DATASET_ID }}
        run: |
          echo "🔐 Getting Power BI access token via MSAL..."
          TOKEN=$(curl -s -X POST "https://login.microsoftonline.com/$TENANT_ID/oauth2/v2.0/token" \
            -H "Content-Type: application/x-www-form-urlencoded" \
            -d "grant_type=client_credentials&client_id=$CLIENT_ID&client_secret=$CLIENT_SECRET&scope=https://analysis.windows.net/powerbi/api/.default" \
            | jq -r '.access_token')

          echo "🚀 Triggering dataset refresh in Power BI..."
          RESPONSE=$(curl -s -w "%{http_code}" -o /dev/null -X POST \
            -H "Authorization: Bearer $TOKEN" \
            -H "Content-Type: application/json" \
            "https://api.powerbi.com/v1.0/myorg/groups/$WORKSPACE_ID/datasets/$DATASET_ID/refreshes")

          if [ "$RESPONSE" = "202" ]; then
            echo "✅ Power BI refresh queued"
          else
            echo "⚠️ Power BI refresh returned status $RESPONSE"
          fi
